spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bidlydb_dev
    username: postgres
    password: postgres
  jpa:
    show-sql: true
  flyway:
    enabled: true

logging:
  level:
    com.bidly: DEBUG
    org.hibernate.SQL: DEBUG
